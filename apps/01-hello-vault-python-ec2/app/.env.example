# Copy to .env for local development — never commit .env
VAULT_ADDR=https://vault.example.com
VAULT_NAMESPACE=admin
VAULT_ROLE=hello-vault-python
SECRET_PATH=config
MOUNT_POINT=apps/hello-vault-python
